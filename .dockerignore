# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output
.next
out
build

# Git / agent tooling
.git
.gitignore
.agents
.claude
.windsurf
skills-lock.json

# Local env — never bake secrets into an image
.env
.env*.local

# Runtime data — bind-mounted via docker-compose (./storage:/app/storage)
storage

# SQLite / local db artifacts
*.db

# TypeScript incremental cache
*.tsbuildinfo
next-env.d.ts

# Deploy docs (not needed in the image)
deploy
